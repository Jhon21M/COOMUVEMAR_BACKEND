language: node_js
node_js:
  - "16"
edge: true
stages:
  - name: build
cache:
  directories:
    - "node_modules"
jobs:
  include:
    - stage: build
      script:
        - npm install
        - npm run build